name: AirTranslate Mac Runner Test

on:
  workflow_dispatch:

jobs:
  smoke:
    runs-on: macos-latest
    steps:
      - name: Verify macOS runner
        run: |
          echo "MACOS_RUNNER_OK=YES"
          sw_vers
          xcodebuild -version
