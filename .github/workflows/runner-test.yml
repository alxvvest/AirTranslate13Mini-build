name: Runner Control Test

on:
  workflow_dispatch:

jobs:
  linux:
    runs-on: ubuntu-latest
    steps:
      - run: echo "UBUNTU_RUNNER_OK"

  mac:
    runs-on: macos-26-intel
    steps:
      - run: |
          echo "MAC_RUNNER_OK"
          sw_vers
          xcodebuild -version
